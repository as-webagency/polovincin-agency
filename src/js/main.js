document.addEventListener('DOMContentLoaded', function () {
  'use strict';
  // запускаем функцию анимации в нужный момент
  // файл анимации должен подключаться выше файла, из которого анимация запускается
  animateForSite()
})